# data branch

Machine-written by .github/workflows/history-snapshot.yml (every 15 min). `history.json` = [unix ts, index price, total NAV, RBDX supply, pool price|null] samples; read by the frontend price chart. Initial contents from script/analytics/backfill-history.js.
